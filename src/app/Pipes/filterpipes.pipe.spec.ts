import { FilterpipesPipe } from './filterpipes.pipe';

describe('FilterpipesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterpipesPipe();
    expect(pipe).toBeTruthy();
  });
});
